import styled from 'styled-components';
import { baseTheme } from '../theme';

export const Container = styled.div`
	max-width: ${baseTheme.media.content};
	height: 100%;
	margin: 0 auto;
	padding: 20px;
`;
